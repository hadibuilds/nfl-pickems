# Development Environment Configuration
DJANGO_ENV=dev
DEBUG=True
INVITE_CODE=dev-invite-code
DJANGO_ADMIN_USERNAME=admin
DJANGO_ADMIN_PASSWORD=admin123
ALLOWED_HOSTS=localhost,127.0.0.1,[::1],localhost:8000,127.0.0.1:8000
VITE_API_URL=http://localhost:8000

# Database (uses SQLite by default for dev)
# DATABASE_URL=  # Leave empty to use SQLite for development

# Storage (local for development)
USE_CLOUD_STORAGE=False

# Email (console backend for development)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Logging (clean development logs)
DJANGO_LOG_LEVEL=INFO